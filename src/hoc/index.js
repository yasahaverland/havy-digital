/* eslint-disable no-unused-vars */
import SectionWrapper from "./SectionWrapper";

// Todos esses index.js nas pastas facilitan a exportação dos componentes, functions e assets por todo o projeto, evitando paths como ../../../../ e outros possiveis bugs que poderiam ocorrer quando você tem um codigo muito grande e sem uma organização apropriada.

export { SectionWrapper }
